# This requirements file lists all dependencies for this project that are not
# already installed by RocketMap.
#
# Run 'pip install -r requirements.txt to install the dependencies for VSnipe.
bottle>=0.12
